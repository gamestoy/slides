//anonymous function
setTimeout(function() { console.log('hi!'); }, 2000);

//arrow function
setTimeout(() => console.log('hi!'), 2000);

const add = (n,m) => n + m;

const points = (price, ratio = 1) => price * ratio;

const toUpperCase = s => s.toUpperCase();

const multiple = s => {
  //cosas
  //cositas
};
