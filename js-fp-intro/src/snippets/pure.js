const rnd = () => Math.random();

const addClassToBody = cn => document.body.className = cn;

const add = (a, b) => a + b;

const log = msg => console.log(msg);

const addElement = (arr, e) => arr.push(e);

const addElement = (arr, e) => [...arr, e];
